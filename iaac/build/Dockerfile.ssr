FROM node:12
ENV PORT=8997

ADD codebase /app/
WORKDIR /app/frontoffice
RUN npm install -g gatsby-cli

ENV ENABLE_GATSBY_REFRESH_ENDPOINT=true
ENV PRISM_BASEURL=http://prism.hec.develop.dev-dropteam.com

EXPOSE 8997
CMD ["yarn", "develop", "-H", "0.0.0.0"]